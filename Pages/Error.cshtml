@page
@model ErrorModel
@{ ViewData["Title"] = "Error"; }

<div class="container py-5 text-center">
    <h2>Something went wrong.</h2>
    @if (Model.ShowRequestId) { <p class="text-secondary mt-3"><small>Request ID: @Model.RequestId</small></p> }
</div>
